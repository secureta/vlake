name: test
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read  # checkout のみ。GITHUB_TOKEN の権限を最小化
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: uv sync
      - run: uv run pytest -v
